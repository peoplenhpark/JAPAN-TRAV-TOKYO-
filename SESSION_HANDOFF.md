# 🗼 도쿄 2박3일 2026 — 세션 핸드오프

> **이 파일만 보고 즉시 작업 가능하도록 작성된 자기완결 문서.**
> 최종 갱신: 2026-08-28

---

## 0. 30초 요약

2인 도쿄 2박 3일(2026-09-24~26)의 **모바일용 인터랙티브 여행 가이드** 프로젝트.
산출물은 **단일 HTML 파일 1개**(`index.html`) — 빌드/서버/의존성/CDN 없음. 브라우저로 더블클릭하면 바로 동작.
요청이 들어오면 `index.html`을 직접 편집하면 된다.

**다낭 프로젝트(`C:\apark\다낭여행2026\`)와 같은 계열이지만 한 가지가 다르다:**
다낭은 Tailwind를 CDN으로 불러오는데, 이 파일은 **CSS를 전부 인라인**했다.
→ 해외에서 데이터 없이도 열리도록 한 의도적 선택. **CDN을 다시 넣지 말 것.**

---

## 1. 위치 & 파일

| 파일 | 설명 | 편집 대상 |
|------|------|-----------|
| `C:\apark\도쿄여행2026\index.html` | 인터랙티브 가이드 (약 88KB, 단일 파일) | ✅ **주 편집 대상** |
| `C:\apark\도쿄여행2026\README.md` | GitHub 저장소 첫 화면 (여는 법·일정 요약·고쳐쓰기) | 🔸 일정/날짜 변경 시 **함께 갱신** |
| `C:\apark\도쿄여행2026\SESSION_HANDOFF.md` | 이 문서 | — |

⚠️ **README와 index.html의 중복 정보**: 날짜·3일 요약표·환율 기본값·파일 크기가 양쪽에 있다.
일정을 고치면 **README의 "일정 요약" 표와 날짜 상수 예시도 같이** 고칠 것.
단 README에는 숙소 번지수를 넣지 않았다(공개 저장소 첫 화면이고 README 용도상 불필요) — 유지할 것.

**원격 저장소**: `https://github.com/peoplenhpark/JAPAN-TRAV-TOKYO-.git`
⚠️ 상태는 §7 참조. **공개(public) 저장소**라 푸시 전 반드시 사용자 확인이 필요하다.

---

## 2. 여행 기본 정보 (콘텐츠 사실관계 — 편집 시 일관성 유지 필수)

- **여행자**: 2인
- **기간**: **2026-09-05(토) ~ 09-07(월)**, 2박 3일 ⚠️2026-08-28에 9/24~26에서 **변경됨**
- **항공**: **나리타 IN/OUT 확정** — 가는 편 **인천 08:00 출발**(나리타 약 10:20 도착) / 오는 편 **나리타 18:00 출발**
  - 접근: 게이세이 **액세스특급** → 아사쿠사선 직통 → 히가시긴자. 약 80분 ¥1,330, 환승 0회. ⚠️**시간당 1~2편뿐**
  - 파생 시각: Day1 숙소 도착 **12:50** / Day3 숙소 출발 **13:30**(15:00 공항 도착 역산)
  - 접근: 게이세이 **액세스특급** → 아사쿠사선 직통 → 히가시긴자. 약 80분 ¥1,330, 환승 0회. ⚠️**시간당 1~2편뿐**
- **숙소**: **Somerset Ginza East Tokyo** (サマセット銀座イースト東京)
  - 주소: `〒104-0045 東京都中央区築地 4-1-12`
  - 히가시긴자역 도보 3분(히비야선·**아사쿠사선**) / 츠키지시조역 도보 5분(오에도선) / 츠키지역 도보 7분 / 긴자역 도보 8분 / 신토미초역 도보 10분
  - 서비스드 레지던스(아스콧 계열) — 객실 타입에 따라 주방·세탁 설비
- **환율 기준값**: **8.7원/엔** (2026-08-21 100엔=868원 기사값 기준). 앱 내에서 사용자 조정 가능

### ⚠️ 사용자 원문과 다르게 해석한 항목 (재확인 필요)

| 사용자 표기 | 해석 | 근거 |
|---|---|---|
| "긴자 캔싱턴 호텔" | → **긴자 서머셋** | 사용자가 대화 중 "긴자 서머셋"으로 정정함. 켄싱턴은 검색 3회 전부 미확인 |
| "일본타워" | → **도쿄 스카이트리** | ✅ **2026-08-28 사용자가 "스카이트리가 맞다"고 확인함.** 해석 안내 문구는 앱에서 제거됨 |
| "아사카사" | → **아사쿠사**(浅草) | 센소지 등 요청 맥락상. 아카사카(赤坂)가 아님 |
| "오사키 메구로강" | → 오사키 + 메구로강 (별개 2지점) | 오사키는 메구로강 하류. **앱에 "오사키 구간은 볼거리 적음"을 명시** |

### 일자별 일정 (schedule 탭)

**설계 원칙 = 하루에 지하철 노선 하나.** 요청지 7곳 전부 환승 0회.

| 날짜 | 강도 | 축(노선) | 핵심 |
|------|------|---------|------|
| 9/5 (토) | 🟡 중간 | 오에도선 | 12:50 짐 보관 → **13:10 츠키지 맛보기**(토=영업) → 혼간지 → 하마리큐 → **긴자 보행자천국** → 18:40 도쿄타워 야경 |
| 9/6 (일) | 🔴 풀데이 | 히비야선+야마노테 | 09:30 에비스 → 오사키 → **14:35 메구로→나카메구로 강변 도보** → 15:15 로스터리(냉방) → 시부야 야경 |
| 9/7 (월) | 🔴 이른시작 | 아사쿠사선 | **06:20 츠키지 본편** → 08:15 체크아웃 → 08:55 센소지 → 10:30 스카이트리 → 11:40 소라마치 → 13:00 짐픽업 → **13:30 나리타行** |

> **왜 이 순서인가** (변경 시 반드시 고려):
> - 츠키지 장외시장은 **05:00~14:00**이라 오후 방문이 물리적으로 불가능 → 아침 고정
> - Day 3을 아사쿠사선 축에 둔 이유 = 그 노선이 **나리타까지 직통** → 출국일 동선이 안 꼬임
> - ⭐⭐ **요일이 츠키지를 결정한다.** 츠키지 장외시장은 **일요일 휴무**인데 **9/6이 일요일**이다.
>   그래서 츠키지가 Day 2 아침이 아니라 **Day 3(월) 06:20**에 있다. 날짜를 또 바꾸면 **가장 먼저 이 제약을 재확인**할 것.
> - ⭐ **Day 3 고정점 = "13:30 숙소 출발".** 나리타 18:00 출발 → 15:00 공항 도착(3시간 전) → 80분 이동 역산.
>   12:40 오시아게 출발 / 13:00 짐픽업도 같이 고정이다. 여기를 건드리면 비행기를 놓친다.
> - 9월 **상순**은 하순보다 덥다(낮 30~32℃·습도 70~75%). 야외는 오전·저녁, 한낮 13~16시는 실내로 배치돼 있다.
> - 도쿄타워(야경) / 스카이트리(낮) 로 나눈 것은 의도적 — 전망대 2곳이 성격이 겹치지 않게

---

## 3. 기술 구조 (편집 가이드)

**스택**: 순수 정적 단일 HTML. **CDN·프레임워크·빌드도구 전부 없음.**
CSS는 `<head>` 인라인 `<style>` 하나, JS는 파일 하단 인라인 `<script>` 하나(IIFE).

### 탭 구조 (10개, 모바일 5×2 그리드)
`<nav>`의 버튼 `data-tab="..."` ↔ `<section id="tab-...">` 매칭.

| data-tab | 라벨 | section id |
|----------|------|------------|
| schedule | 📅 일정 | `#tab-schedule` |
| spots | 🗼 명소 | `#tab-spots` |
| places | 🍣 맛집 | `#tab-places` |
| transit | 🚇 교통 | `#tab-transit` |
| map | 🗺️ 지도 | `#tab-map` |
| money | 💴 환율 | `#tab-money` |
| phrase | 🇯🇵 회화 | `#tab-phrase` |
| check | ✅ 준비물 | `#tab-check` |
| budget | 💰 예산 | `#tab-budget` |
| emergency | 🚨 비상 | `#tab-emergency` |

### 주요 JS 동작 (파일 하단 IIFE)
- **날짜 상수**: 스크립트 최상단 `TRIP_START` / `TRIP_END` / `TRIP_DAYS` **3줄뿐**.
  ⭐ 날짜 변경 시 여기 + 본문 텍스트만 고치면 된다 (다낭 파일처럼 리터럴이 흩어져 있지 않음).
- **탭 전환**: `data-tab` 클릭 → 해당 section에 `.on` 토글
- **D-day**: 출발 전 `D-N` / 여행 중 `DAY n` / 이후 `완료` 자동 분기
- **도쿄 시계**: UTC+9로 직접 계산(기기 표준시 무관). 한일 시차 0이라 단일 시계
- **다크모드**: `#darkToggle` → `<html class="dark">` 토글, `localStorage: tokyo_dark`
- **환율 계산기**: `#rate`(기본 8.7) / `#jpy` ↔ `#krw` **양방향 바인딩**, `localStorage: tokyo_rate`
  - 헤더 `#rateHd`, `#per100`, 빠른표 `#qtbl`이 rate 변경 시 함께 갱신 (`syncRate()`)
- **일자 아코디언**: `.day-hd` 클릭 → `.day.closed` 토글
- **체크리스트**: 카드 `id` + 인덱스로 `localStorage: tokyo_{cardId}_{i}` 저장, `#resetChk`로 초기화
- **인쇄**: `@media print` — 모든 탭·모든 아코디언 펼쳐서 A4 출력

### 편집 팁
- 82KB 단일 파일이므로 **Grep으로 위치 먼저 찾고 Edit**. 통째 Read 비효율.
- ⭐ **`localStorage` 접근은 전부 `store.get/set/del` 래퍼를 쓸 것.** try/catch가 들어 있어 프라이빗 모드·저장소 차단 환경에서도 페이지가 죽지 않는다. 직접 `localStorage.xxx`를 호출하지 말 것.
- 요금·시간은 본문 여러 탭(일정/명소/예산)에 **중복 산재** → 변경 시 grep으로 전부 동기화.
- 색상/강도 뱃지: 🔴풀데이 🟡중간 🟢자유 컨벤션 유지 (다낭 파일과 동일).

### 🎨 색상 시스템 (2026-08-28 개편)
초기의 진홍(#8c1d18) 계열에서 **먹빛 남색(藍) + 황동** 으로 교체했다. 채도를 낮추고 대비를 정리한 쪽이 차분하다.

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| `--accent` | `#2c4a63` 남색 | `#8bb4d6` | 헤딩·활성탭·타임라인 점·시각 |
| `--accent2` | `#7a5730` 황동 | `#d4b478` | 링크·`.b-rec` 뱃지 |
| `--ink3` | `#75767f` | `#8e8f99` | 보조 텍스트 |
| 헤더 그라디언트 | `#33556f → #25394d → #1a1d26` | 동일 | 황동 글로우 오버레이 |

⭐ **반드시 지킬 두 가지:**
1. **`--accent-r` / `--ok-r` / `--warn-r` RGB 트리플을 함께 유지할 것.** `.chip`·`.note`의 반투명 배경이 `rgba(var(--accent-r),.11)` 형태로 이 값을 쓴다. accent 색을 바꾸면 **트리플도 같이 바꿔야** 배경색이 어긋나지 않는다.
2. **다크모드는 accent가 밝아지므로 그 위 글자를 어둡게 반전**시켜야 한다:
   `html.dark .navgrid button.on, .day-hd .dnum, .b-must, .b-rec { color:#12141a }`
   이 규칙을 지우면 다크모드에서 흰 글자 위 밝은 파랑이 되어 **대비 2:1로 읽을 수 없게 된다.**

**대비 검증 결과(2026-08-28 실측)**: 전 토큰이 카드 배경 대비 **WCAG AA(4.5:1) 통과** — Light 4.51~17.04 / Dark 5.32~14.10, 미달 0건.
색을 바꾸면 이 기준으로 재검증할 것(야외 햇빛에서 보는 가이드라 실사용상 중요).
- 배지 클래스: `.b-must`(요청지) / `.b-rec`(추천, 동선에 편성됨) / `.b-opt`(옵션).

### ⚠️ Bash heredoc 주의 (이 세션에서 실제로 겪음)
이 크기(8KB+)의 HTML을 `cat > file << 'EOF'` 로 쓰면 이 환경에서 **`unexpected EOF` 로 실패**한다.
(작은 heredoc은 정상 동작 — 크기 문제로 추정.) **파일 작성은 Write/Edit 도구를 쓸 것.**

---

## 4. 현재 상태

- ✅ 10개 탭 콘텐츠 완비, 모바일 최적화 + 다크모드 + 인쇄 스타일
- ✅ D-day/시계/환율계산기/체크리스트/아코디언 **브라우저 실측 검증 완료**
  - ¥2,500→21,750원, 100,000원→¥11,494, rate 9.5 변경 시 전 항목 재계산 정상
  - 탭 10 ↔ 섹션 10, **고아 참조 0건**
  - 콘솔 에러 0
- ⏳ **열린 항목**: §6 참조 (공항 확정, 시부야스카이 예약 여부, GitHub 푸시)

---

## 5. 검증 방법

```powershell
start "C:\apark\도쿄여행2026\index.html"
```
체크: ① 10탭 전부 전환 ② D-day 카운트다운 ③ 다크모드 토글·새로고침 후 유지
④ 환율 탭에서 엔/원 양방향 입력 ⑤ 준비물 체크 후 새로고침 시 유지
⑥ Ctrl+P 인쇄 미리보기에서 모든 탭·아코디언 펼쳐지는지.

> ⚠️ 브라우저 프리뷰 도구로 볼 때: `C:\apark\`는 작업 폴더 밖이라 정적 스냅샷으로 뜬다.
> JS까지 검증하려면 `C:\Users\peopl\` 아래로 복사해서 열 것 (검증 후 삭제).

---

## 6. 열린 항목 (사용자 확인 대기)

| # | 항목 | 왜 중요한가 |
|---|------|------------|
| 1 | ~~하네다 vs 나리타~~ | ✅ **나리타 확정 (2026-08-28)**. Day 3 전면 재편성 완료 |
| 2 | ~~정확한 항공편 시각~~ | ✅ **확정 (2026-08-28)**: 인천 08:00 출발 / 나리타 18:00 출발. 편명만 미확보 |
| 3 | **시부야 스카이 예약 여부** | 일몰 슬롯은 몇 주 전 매진. 현재 Day 2 **옵션**으로만 배치 |
| 4 | **긴자 오마카세 계획** | 예산이 2인 26~70만원 갈림. 유명 업장은 수개월 전 예약 |
| 5 | **GitHub 푸시** | §7 |

---

## 7. GitHub 저장소

- URL: `https://github.com/peoplenhpark/JAPAN-TRAV-TOKYO-`
- **✅ 푸시 완료 (2026-08-28)** — `main` 브랜치, 파일 3개(`index.html` 82,119B / `SESSION_HANDOFF.md` / `.gitignore`)
- 상태: **public** (사용자가 공개 유지를 명시 선택). `default_branch=main`
- 환경: `git` 2.54 / **`gh` CLI 없음** / credential helper = `manager`(GCM, 자격증명 저장돼 있어 push 무인 동작)
- `user.name`/`user.email` 전역 미설정 → **repo-local로 설정해 둠** (`peoplenhpark` / `chris@cutback.video`)

### 공개 범위 — 이미 고지·승인된 사항
public이라 아래가 인터넷에 검색된다. 2026-08-28에 사용자에게 고지했고 **사용자가 공개 유지를 선택**했다.
- 숙소 실명·정확한 주소 (`東京都中央区築地 4-1-12`) / 여행 날짜 (**9/5~7**) / 인원(2인) / 항공 시각

⛔ **다시 문제 제기하지 말 것** — 결정이 끝난 사안이다. 단, 이후 편집에서 **새로운 종류의 개인정보**(항공편 예약번호, 여권정보, 연락처, 결제수단)를 추가할 때는 별도로 확인할 것.

> 참고: 메모리 [[feedback-no-remote-push]]는 **투자 repo(`telegram_summary`) 전용** 규칙이라 이 프로젝트에는 적용되지 않는다.

### GitHub Pages — 아직 미활성 (`/pages` API 404)
활성화하면 휴대폰에서 URL로 바로 열 수 있다. **웹 UI에서만 가능**(`gh` 없음, API 활성화는 인증 토큰 필요):
Settings → Pages → Source: **Deploy from a branch** → Branch: **main** / **/(root)** → Save.
→ 배포 URL: `https://peoplenhpark.github.io/JAPAN-TRAV-TOKYO-/` (저장소명 끝의 하이픈 포함)

---

## 8. 흔한 후속 작업 시나리오

| 요청 | 작업 위치 |
|------|-----------|
| 여행 날짜 변경 | JS 상단 `TRIP_START`/`TRIP_END` + `<header>` 부제 + 일정 탭 각 `.day-hd` |
| 일정/시간 변경 | `#tab-schedule`의 해당 `.ev` 블록 |
| 명소 추가 | `#tab-spots` `.card` 복제 후 교체 (배지 클래스 확인) |
| 맛집 추가 | `#tab-places` 지역별 `h3.grp` 아래 `.card` 복제 |
| 환율 기본값 변경 | `#rate`의 `value` 속성 + `getRate()` 폴백값 `8.7` **양쪽** |
| 준비물 추가 | `#tab-check` `.chk` 라벨 복제 ⚠️ **기존 항목 사이에 끼워넣으면 인덱스가 밀려 저장된 체크가 어긋난다 → 끝에 추가할 것** |
| 예산 수정 | `#tab-budget` + 명소/일정 탭의 중복 요금 동기화 |
| 지도 링크 추가 | `#tab-map`, 형식: `https://www.google.com/maps/search/?api=1&query=...` (HTML에선 `&amp;`) |

---

## 9. 관련 메모리

- 영구 메모리: `C:\Users\peopl\.claude\projects\C--Users-peopl\memory\reference_tokyo_trip_plan.md`
- 자매 프로젝트: 다낭 `C:\apark\다낭여행2026\` (`reference_danang_trip_plan.md`)
- 사용자 언어: **한국어** (응답 한국어).
